import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SettingsSourcesPage() {
  return (
    <PlaceholderPage
      active="/settings"
      title="Sources."
      milestone="Milestone 2 — connect Google Contacts, Gmail, Calendar, LinkedIn CSV, Mac agent here"
    />
  );
}
